# Board — make-skill

What this repository knows it owes. A finding that arrives from outside — a
cross-repository audit, a sibling's incident — lands here with the id it arrived
under, so the row can be closed against the same name in both places.

**Priority is computed, not felt.** `P = blast × (1 + age_runs) / effort`, where
*blast* is who is hurt if it stays (3 = a user of the pack, 2 = the operator of
this machine, 1 = a future run of this repo), *age_runs* is how many **distinct
days** carrying a run stamp the row has survived, and *effort* is rough size
(1 = under an hour, 2 = a session, 3 = its own run). Recomputed when the row is
touched, never inherited, so a row cannot keep a rank it earned when it was new.

**Ids are not minted here.** `.claude/agent-sync.json` declares no id register —
`docs/AGENT_SYNC.md` says ids live in the parent repository. A row therefore
carries the id of whatever registered it: `B-nn` from the `sshlg-skills` board,
`M-nn` for a Proof-of-Done manifesto requirement, `MS-nn` for a row of the
cross-repository conformance program. Inventing a local scheme beside those is
how the same defect ends up closed twice under two names.

| id | What | Source | Blast | Age | Effort | P | Status |
|---|---|---|---|---|---|---|---|
| MS-01 | **The gate left 47.3 MB of temp state on the machine and never said a word about it.** `test/checker_parity_test.py:60,160` called `tempfile.mkdtemp()` to plant a defect into a copy of this whole repository and removed neither the copy nor the planted skill dir; `test/plant_guard_test.py:34` did the same per case. Measured under `$TMPDIR` on 2026-08-19: **60 `planted/` skill dirs and 36 `repo/` copytrees** unambiguously this repository's, plus **1792 plant-guard trees** whose fixture is byte-identical in four family repositories — **1888 abandoned directories, 47.3 MB as sampled that day**, over roughly 220 gate runs — recounted 2026-08-20 as **2688 directories, 83.6 MB**, of which this repository's own two leaks contributed **0** new trees since the fix landed (MS-04). The leak is the cheap half. The defect named by manifesto requirement **M-49** is that a completed run *recorded* nothing, so the next leak is invisible in exactly the same way: no test in the family printed what it left, and M-49 scored zero FULL across all nine members. | 2026-08-18 Proof-of-Done conformance audit, requirement M-49 | 2 | 0 | 1 | **2.0** | **closed 2026-08-19, shipped in v0.22.0** — `16a9682`, released as `fc25f4a` / tag `v0.22.0` (npm `0.22.0`, published 2026-08-19T19:27:15Z) and **re-confirmed on that artifact 2026-08-20**: `git archive v0.22.0` into a clean tree, all four gate commands exit 0 with their residue lines, and the three `find` counts unchanged by the run. `test/residue.py` is the ledger every temp tree in this suite now goes through, and **every one of the four gate commands ends with one line naming its residue, `nothing` included** — that line, not the cleanup, is what makes the next leak show up in the gate's own output. A case that fails, or raises anything that is not an assertion, **keeps its workspace on purpose**: a planted defect is debugged by reading the tree it landed in, and a cleanup that runs only on the pass path deletes the evidence exactly when it is wanted; the report then names the path, the case that owns it, and the `rm -rf` that ends it. `test/residue_test.py` (8 cases) joins `npm test` last and was **watched failing 2 of 8** against the pre-fix suites, with the two e2e boxes kept — the failure path demonstrating itself. Workspaces now carry a `make-skill-test-` prefix, because the 1888 directories this closed are plain `tmpXXXXXXXX` and that namelessness is why they were never swept. **The 1888 were counted and left in place**, per `manifesto.md:366`: 1792 of them are indistinguishable between this repository, `sshlg-skills`, `agent-stack` and `seo-aeo-audit`, all four of which ship the same `tempfile.mkdtemp()` fixture at line 34 — the pile grew by 8 while it was being measured, from a sibling's run. **That referral was the open half; it is closed as of 2026-08-20, and not by this repository** — see the MS-02 row below. |
| MS-02 | **The referral: three siblings shipped the identical leaking fixture.** `test/plant_guard_test.py:34` called `tempfile.mkdtemp()` with no cleanup in `sshlg-skills`, `agent-stack` and `seo-aeo-audit` as well as here, so no plain `tmpXXXXXXXX` tree could be attributed to any one of them and none of them could be swept. | MS-01's open half, 2026-08-19 | 2 | 1 | 1 | **4.0** | **closed 2026-08-20, by three other agents — recorded here, not claimed.** Every copy of the fixture in the family now routes its trees through a `residue` ledger: `grep -c 'import residue'` → **1** and `grep -c 'tempfile.mkdtemp()'` → **0** in all four. `sshlg-skills` at `b371301` (2026-08-20T01:11:45+02:00), `seo-aeo-audit` at `c859cba` (2026-08-20T01:31:48+02:00), and `agent-stack` in the same session as this row, which **ported** `test/residue.py` from here rather than reinventing it. The pile itself stays: 2688 directories on 2026-08-20, plain-named and ambiguously owned, reported and left alone per `manifesto.md:366`. |
| MS-05 | **This suite has no check that a temp tree went through the ledger.** `residue.report()` accounts only for what `workspace()` handed out, so a bare `tempfile.mkdtemp()` re-introduced anywhere in `test/` is invisible and the gate prints *"this run left nothing — 0 temp tree(s) created"* over a live leak. Watched happening: `agent-stack` re-planted exactly that during its port and the fixture **passed**, exit 0, ledger clean. A bypass that reports clean is indistinguishable from no leak, which is the reading MS-01 exists to prevent — one scope smaller. | AG-06's port of this repository's `residue.py`, 2026-08-20 | 2 | 0 | 1 | **2.0** | **open.** The mechanism exists and is proven: `agent-stack/test/validate.py`'s `check_temp_trees_go_through_the_residue_ledger` refuses `mkdtemp` and `TemporaryDirectory` in any `test/*.py` but `residue.py`, reading source with comments and docstrings stripped, with its pattern assembled from parts so the check does not exempt itself by accident. Porting it back here is a code change to a suite this run was scoped to document, so it is filed rather than done quietly. |
| MSK-01 | **The eval suite is authored and has never been executed against a model.** `test/evals/RESULTS.md` says so in its own status line; the results table holds one row of dashes. Shape is validated on every commit; behaviour has been measured zero times, so nothing yet proves the skill triggers, stays quiet on a near-miss, or performs the steps it documents. | 2026-08-30 family audit (MSK.md), MSK-01 | 2 | 1 | 3 | **1.3** | **closed 2026-08-31, shipped in v0.25.2** — 22 blind trigger probes per model (haiku 22/22; sonnet 20/22, the two misses named) plus all 4 scenarios scored per line (sonnet 31/35, haiku 24/35), recorded as dated rows in `test/evals/RESULTS.md` with a Method section stating the harness limits: one probe per query, Agent-tool subagents on a machine whose global config routes toward the family, model aliases not ids, two probes carrying a non-compliance caveat. The s01 artifacts were re-audited with the bundled auditor rather than trusted from the agents' reports. A clean-install replication is still owed and RESULTS.md says so. |
| MSK-02 | **A hand-typed eval count drifted: RESULTS.md said "20 queries" and SKILL-CARD.md "20 trigger queries" over a `triggers.json` holding 22** — the exact class SKILL.md's gotcha list forbids, and `evals_validate.py` checked only existence and shape, never the prose against the artifact. | 2026-08-30 family audit (MSK.md), MSK-02 | 1 | 0 | 1 | **1.0** | **closed 2026-08-30, shipped in v0.25.1** — both numbers corrected AND both statements put under comparison: `test/evals_validate.py` parses RESULTS.md's stated counts against the artifacts (claim-free file refused, off-by-one plant caught in `--self-test`), `test/validate.py` COUNTED_CLAIMS gains the `N trigger queries` / `N behavioural scenarios` patterns. Both guards watched failing against the real 20-vs-22 defect before the fix. |
| MSK-03 | **The discovery check at SKILL.md:271 (`npx skills add --list`) runs nowhere** — blocked locally by the umbrella's hygiene guard, absent from CI, so nothing asserts the published listing serves exactly one skill. | 2026-08-30 family audit (MSK.md), MSK-03 | 1 | 1 | 2 | **1.0** | **closed 2026-08-31, shipped in v0.25.2** — `skills-cli-discovery` job in `.github/workflows/validate.yml`: runs the skills-CLI `--list` against `ssheleg/make-skill`, strips ANSI, asserts `Found 1 skill` AND that the one name is `make-skill`, with a remedy line on each failure branch. Its own job so an upstream outage cannot mask the offline suite; reads the default branch, so the post-merge push and the release's `workflow_call` on the tag assert the published tree. Deliberately NOT runnable on this machine — the umbrella's hygiene guard refusing the bare skills CLI on family members is why the check had nowhere to run before CI. |
| MSK-04 | **No headroom for the next correction: body ~4742 tokens against the 4750 working limit (`BODY_TARGET_TOKENS`, test/validate.py:203) — 8 spare; description 965/970 chars — 5 spare.** The next forced edit to SKILL.md has nowhere to go. | 2026-08-30 family audit (MSK.md), MSK-04 | 1 | 1 | 2 | **1.0** | **closed 2026-08-31, shipped in v0.25.2** — displacement, not deletion: the Cyrillic budget measurement → `authoring.md` *Content guidelines*, the six skeleton filenames → `distribution.md`'s layout tree, the five-facts and machine-refresh sequences now referenced at their single home in the checklists, the long-file rule pointed at where it already lived. Auditor before/after: ~4742/4750 (8 spare) → **~4677/4750 (73 spare)** — after also paying for XF-10's two table-row additions. Description untouched at 965/970: its 5 spare chars are spent only on trigger changes, and this release makes none. |
| B-122 | **The skill told authors for four weeks that no built-in eval runner exists, and prescribed a home-rolled suite on that basis.** `references/authoring.md:245` read *"there is no built-in runner, so keep them in the repo as data and run them yourself"*; `claude plugin eval` had shipped, and its `--ablation with-without` performs steps 3 and 5 of the same paragraph's own procedure. Two derived statements carried the error: `distribution.md`'s *"run by a human"* and `retrofit.md`'s item 8, which asked only that evaluations *exist* — the state MSK-01 had just closed. The sentence violated the skill's own rule *"No time-sensitive statements"*, written 100 lines above it in the same file, and nothing enforced that rule. | 2026-08-31 harness-engineering harvest (awesome-harness-engineering + ai-agent-book, 375 findings); found by an external reader, not by house review | 3 | 0 | 1 | **3.0** | **closed 2026-08-31, shipped in v0.25.3** — the replacement is a measurement rather than a second absence: on Claude Code 2.1.236 every `claude plugin eval` path prints ``plugin eval` is currently in early access`, writes nothing and exits 1, with no settings key, env var or feature flag enabling it, so `test/evals/` remains what runs and the paragraph names the day to migrate. `THIRD_PARTY_CLAIMS` in `test/validate.py` closes the class: a registered claim about someone else's tool must keep its re-check command and an ISO date within eight lines, and a row whose claim has been deleted from the doctrine fails too, so the registry cannot outlive it. A generic absence-claim detector was built and **measured first**: 10 hits over the shipped doctrine, ~half of them legitimate (the `host-capabilities.md` fallback rows are that sentence shape by design), so it was rejected as over-defense and the reasoning kept in the code. Three plants added to the existing CI self-test group, keeping `grep -c 'name: Negative self-test'` at 9. **The plants passed for the wrong reason on the first run** — the claim wraps a line break and the matcher was line-anchored, so all three tripped the registry-rot branch; whitespace-tolerant now, each asserting its own message. |
